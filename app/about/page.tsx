import { Inter, Montserrat } from "next/font/google";

const mont = Inter({ subsets: ["latin"] });

export default function about() {
  return (
    <main className="bg-clr-200 flex min-h-screen flex-col items-center justify-center">
      about
    </main>
  );
}
